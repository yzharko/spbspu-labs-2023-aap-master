#ifndef ADDSIZELINE_HPP
#define ADDSIZELINE_HPP
#include <cstddef>

namespace nikiforov {
  void AddFourElem(double*& arr, size_t& countElem);
  void AddThreeElem(double*& arr, size_t& countElem);
}

#endif
