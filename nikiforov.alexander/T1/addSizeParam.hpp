#ifndef ADDSIZELINE_HPP
#define ADDSIZELINE_HPP
#include <cstddef>

namespace nikiforov {
  double* AddFourElem(double* arr, size_t& countElem);
  double* AddThreeElem(double* arr, size_t& countElem);
}

#endif
