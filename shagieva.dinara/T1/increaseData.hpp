#ifndef INCREASE_DATA
#define INCREASE_DATA
#include "shape.hpp"
#include <cstddef>

namespace shagieva
{
  void increaseData(Shape ** shapes, size_t & capacity, const size_t & shapeCount);
};

#endif
